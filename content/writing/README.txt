# Blog posts

Drop Markdown files here to publish them at `/writing/<filename>`.

Each file needs frontmatter:

```yaml
---
title: My Post Title
date: '2026-01-01'
description: One-line summary shown on the index and in metadata.
draft: false # optional; drafts only render in `next dev`
---
```
